# R packages

Final governed execution used R 4.5.1 with:

```r
install.packages(c(
  "survey",
  "survival"
))
```

The BioAge package is required for the original-model agreement checks.
Install it according to the package's maintained installation instructions.

Recorded final versions:

- `survey` 4.5
- `survival` 3.8-3
